LOCUS VALIDATION — PHASE 13 (The Live Wire: estate → Hermes skill module + first session)
==========================================================================================
Role: Locus (validator-steward) — independent validation: internal consistency and
discipline compliance, not quality. Date: 2026-08-07.

Artifacts validated (read in full):
  - Phase13_Plan.md (contract; Dallas directive: "proceed with assumptions")
  - ~/.hermes/skills/motivational-ecology/SKILL.md
  - ~/.hermes/skills/motivational-ecology/scripts/run_gate.py
  - ~/.hermes/skills/motivational-ecology/scripts/run_dojo_session.py
  - Phase13_Wiring/verify_phase13.py
  - Phase10_Integration/faos_ecology_config.yaml (absence register)
  - Phase10_Integration/faos_ecology_engine.py (quarantine + close code)
  - GitHub_PoC/logs/live_session_20260807-221115Z/session.json AND
    GitHub_PoC/logs/live_session_20260807-221811Z/session.json (two records on disk)
  - GitHub_PoC/routines/Ambivalence_Dojo/dialogue_state_machine.json (guard cross-check)

Cross-checks: council_notes/verify_all.py (14-verifier chain, incl. verify_phase13);
council_notes/verify_phase7.py (10 packages, T2R 52); Hermes_Agent_Harness mirror
(10 packages incl. Material_Arrangement_Scan + Feedback_Ecology_Map, docs/architecture.md,
meld/witness_run_phase9.py + witness_run_all_dojos.py, skill_graph_index.json,
lattices/T2R_traceability.json); `hermes skills list` (motivational-ecology enabled);
skill references/ dir (expected estate-map.md).

Grounding (real execution, not static reading):
  - verify_phase13.py run 1 (state where only 221115Z record existed): 31/31 PASS, exit 0
  - verify_phase13.py run 2 (current on-disk state): [FAIL] "session: honest note
    (platform demonstration, not overclaim)" — 1 FAILURE, exit 1
  - run_gate.py exercised via verifier subprocess: coaching ctx → SCAFFOLD; --manipulative → STOP;
    --q-use Q2 without confirmation → blocked (exit 1, "requires explicit user confirmation")
  - T2R_traceability.json: 52 entries (49 instantiated variants + 3 REGISTERED-NOT-BUILT NPT)
  - Engine quarantine code read (faos_ecology_engine.py lines 270-304, close builder 322-392)

CHECK 1 — DECISION FIDELITY: ADMISSIBLE
  Skill module at ~/.hermes/skills/motivational-ecology (SKILL.md + 2 scripts) present and
  enabled (`hermes skills list`); estate referenced IN PLACE (skill dir holds zero estate copies);
  mirror synced (10 packages, docs/, witnesses, graph index, T2R 52); absence entry honest —
  class=human-session-pending, NOT overclaimed as recorded (the plan's literal "→ recorded" flip
  was deliberately revised to the honest state; deviation documented in verify_phase13 docstring,
  absence blocks text, and the 221115Z record — consistent with the phase brief's expectation).

CHECK 2 — INTERNAL CONSISTENCY: REVISE
  Two session records contradict each other: newest 221811Z claims "clears the absence-register
  entry post_meld_live_llm_session" while the absence register says human-session-pending and the
  cited 221115Z record says "remaining runtime act"; committed run_dojo_session.py carries the
  overclaiming canned note + run label (run=phase13-first-live-session) that generated 221811Z;
  verify_phase13 now FAILS its honest-note check on sessions[-1] (exit 1).

CHECK 3 — EVIDENCE LADDER: REVISE
  221115Z record is clean (gates_fired rules match the dojo JSON; close evidence derives from
  session input per engine R6; note self-labels a platform wiring demonstration), but 221811Z's
  note asserts "clears the absence-register entry" — a fact absent from any input (the driver is
  deterministic; no genuine human/LLM session occurred) — canned string from the script.

CHECK 4 — QUARANTINE DISCIPLINE: ADMISSIBLE
  Two-typed load-bearing rule enforced in CODE: assert_use_licensed checks BOTH axes, unknown
  tiers raise FaosConfigError, user-rejected/Q3 is final (engine 270-304); run_gate.py wires
  --user-rejected/--user-confirmed; executed: manipulation→STOP, Q2-without-confirmation blocked.

CHECK 5 — NON-OPERATIONAL REGISTRY: ADMISSIBLE
  Sweep of Phase13_Wiring/, skill module, and both session records: no temporal-echo or
  planetary-hour framing used as evidence; registry entries carried in config with enforcement
  (assert_operational in faos_engine_extension.py); Victor-method 5..1 weights are classification
  weighting, not symbolic evidence — not flagged.

CHECK 6 — PREMATURE COHERENCE: REVISE
  Honest core stands (absence register mirrors the architectural-not-runtime boundary;
  SKILL.md's governance promise is the enabled module's design; 221115Z self-limits), but the
  221811Z record claims more than the phase delivers ("entry cleared" while the genuine live
  session is pending), and verify_phase13 — which checks exactly what it claims — fails on it.

CHECK 7 — CLOSE COMPLETENESS: REVISE
  D1: SKILL.md + run_gate.py + run_dojo_session.py present, but references/estate-map.md MISSING
  (SKILL.md dangles "See references/estate-map.md"; verifier does not check it); D2 gate wired
  (enabled, scripts run); D3 session record + absence update present (honest state); D4 mirror
  synced; D5 verify_phase13.py exists and is chained in verify_all (14 verifiers) but currently
  exits 1 on the stale 221811Z record; legacy suites green.

CONCRETE ACTIONABLES (REVISE):
  1. run_dojo_session.py: change canned run label to "phase13-platform-wiring-demonstration" and
     the note to the honest framing (match 221115Z + absence register + verify check); fix
     docstring's "clears the absence-register entry" claim.
  2. Replace or remove the 221811Z session record (overclaiming, breaks the gate); keep 221115Z
     (the absence register's cited anchor) as the canonical record; re-run verify_phase13 → exit 0.
  3. Add references/estate-map.md (plan D1 deliverable; resolves SKILL.md dangling reference).

LOCUS VERDICT: REVISE
