## Evaluation Notes
Assess this skill using the following checks:

### Fidelity checks (how to know it worked)
- Did every grid cell separate quote, construct code, confidence, and
  implication (no blended verdicts)?
- Were construct codes assigned with confidence weights honoring Cane 2012
  provenance — low confidence where the domain's own validation was fragile?
- Was the barrier grid salience-ordered with the facilitator grid kept
  separate and protected?
- Did the 14→3 COM-B compression produce a binding constraint without hiding
  the domain-level story?
- Did version_metadata preserve the 12-vs-14 witness conflict (Michie 2005 vs
  Cane 2012) — never harmonized?
- Were identity-level claims (domain 3) held in confirmation_queue until
  explicit user confirmation?
- Did it avoid the Knowledge-dominance error (over-weighting Knowledge codes
  despite Knowledge not being an independent cluster in Cane's own data)?

### Empowerment / atrophy / quarantine checks
- Was every code labeled coder-level inference, user-correctable?
- Did the user interpret the grid, with the agent explaining rather than
  concluding?
- Was any identity-level reading quarantined until confirmation — always?
- Did the user remain able to distinguish their own quoted statements from the
  agent's coded interpretation (attribution clarity)?
- Was the grid taught as a vocabulary (fade to self-coding) rather than
  delivered as a black-box verdict?

### VERIFIED evidence anchors
- 14 domains / 84 constructs from Cane 2012 (full text; 37-expert card sort,
  112 constructs). A coded grid should be reproducible from the construct
  inventory; codes not traceable to a Cane construct are suspect.
- Cane's own validation was weak by stated thresholds (silhouette 0.29 < 0.50)
  — grids must encode this fragility instead of radiating certainty.
- Wu 2024 (40 clinicians, 19 barriers / 7 enablers) — real-world grids run
  barrier-heavy; a grid with no facilitators should trigger a check for
  missing positive evidence.
- Zhou 2024 (1382 pubs) — domain coding is a mature practice; cite domain
  definitions from the canonical source, not from memory.
- 12→14 domain history: Michie 2005 (abstract) vs Cane 2012 (full text) — the
  witness conflict is VERIFIED and must be preserved as metadata.

### Calibration loop
- Log user corrections against construct codes; correction rate above ~20%
  (RECONSTRUCTED threshold) indicates coding drift.
- Track domain salience across sessions (N-of-1): stable top-3 salience after
  an intervention suggests the grid should be re-run, not the intervention
  repeated.
