# Feedback Ecology Map — Evaluation Notes

## What verified (deterministic, no LLM)

- Package schema conformance: skill_node.json (layer 2, role
  embedding_map), 2 atomic ops, edge_map (3 internal + 3 compatible_with),
  state_schema (6 keys), 9-file package set.
- Atomic op schema: every op carries id/name/template/description/
  arguments/inputs/outputs/guardrails.
- Boundary discipline: the user defines meaning — agent asks, never
  supplies; CMO hypotheses are claim-level hypotheses, never verdicts.
- T2R register: assess_coherence + form_cmo_hypothesis flipped to
  instantiated.
- skill_graph_index: COMB→FEM, TDF→FEM (and MAS→FEM) compatible_with
  edges un-quarantined (Phase 12 activation).

## What remains unverified (honest ceiling)

- Live-LLM behavior of the ops (absence register blocks runtime sessions;
  the estate's runtime is architectural).
- assess_participation / assess_collective_action / assess_monitoring
  are REGISTERED-NOT-BUILT (runtime signals not yet generated) — the
  package is honest that embedding-work beyond coherence is not yet
  assessed.
- Real NPT longitudinal evidence digest beyond the estate's T2R rows
  (the estate carries the lens; deeper synthesis is wiki-side).

## Stand-in limits (Phase 12 revision round, judge W5)

- Internalization detection uses a small word-marker set ("just what I
  do", "part of me", "i'd feel off", "it's me") — a deterministic
  stand-in for real coherence assessment (LLM-side in production).
- The MAS→FEM compatible_with edge rationale cites the collective-action
  picture, which the package does not assess (coherence is the only
  mechanism built; the other three are registered-not-built).
- Examples showing embedding trajectories are ILLUSTRATIVE of the
  intended capability; the deterministic executable assesses coherence
  only.

## Evaluation checklist (per estate convention)

- Did the package keep meaning user-owned? — yes, ask-never-supply rule.
- Were CMO hypotheses kept at hypothesis level? — yes, claim-level only.
- Did the package claim mechanisms it can't assess? — no; three
  mechanisms explicitly registered-not-built.
