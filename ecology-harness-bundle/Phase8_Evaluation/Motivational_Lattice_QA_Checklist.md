# Motivational Lattice QA Checklist — Phase 8 (Applied)

Project: Motivational Ecology Agent Architecture — Phase 8: Evaluation and Calibration
Date: 2026-08-06
Status: APPLIED against the lattice layer: `lattice_index.json`, `skill_lattice_interface.md`, `insight_trigger_policy.md`, `T2R_traceability.json`, the three MLG schemas (`observation_schema.json`, `insight_node_schema.json`, `evidence_edge_schema.json`), `Motivational_Lattice_Generator/insight_validation_protocol.md`, and the MLG edge_map. Each check names the exact file(s) that answer it, the pass condition, and enforcement status (verified by <verifier> check N, or **GAP**).

Lattice discipline (lattice_index.json `evidence_discipline`): insights are hypotheses, not facts; identity-level claims default to quarantine; the user retains revision, rejection, quarantine, and correction rights. All checks below test whether the artifacts actually enforce that sentence.

---

## L1. Insight hypothesis-status

| Check | Evidence file(s) | Pass condition | Enforcement |
|---|---|---|---|
| Every insight is a hypothesis, never a fact | `insight_node_schema.json` (`layer` const `"interpretation"`; `hypothesis` field), `lattice_index.json` interpretation layer (`required` includes `hypothesis`), MLG ops `form_interpretation_hypothesis` + `separate_inference_layers`, `insight_validation_protocol.md` Gate A #5 ("worded as hypothesis — never 'The user is…'") | Schema constrains layer to interpretation and requires hypothesis wording; validation protocol forbids fact-framing; four layers (observation/interpretation/implication/action) separated | Schema existence + layer keys verified by verify_phase4.py check 10 (lattice layers + schema refs) and check 2 (JSON valid). The *wording* rule (Gate A #5) is **GAP** — no verifier reads hypothesis phrasing or the separate_inference_layers invariant |
| Hypothesis status propagates to consumers | COMB `label_hypothesis_status` op (always-true `hypothesis_status` state), TDF `hypothesis_status: true` (coder-level inference), MI reflections-as-hypotheses, `skill_lattice_interface.md` §3 rule 1 ("No insight enters the skill graph as fact") | Every consuming skill marks profile/grid/reflection as hypothesis; interface rule gates lattice→skill flow | Op existence verified by verify_packages.py check 5; interface rule presence by verify_phase4.py check 1 (output exists) — rule *enforcement* **GAP** (declared, not executed) |

## L2. Quarantine law

| Check | Evidence file(s) | Pass condition | Enforcement |
|---|---|---|---|
| Q2: identity-level claims default to quarantine | `lattice_index.json` quarantine_tiers `Q2_IDENTITY_LEVEL` ("default quarantine; requires explicit user confirmation before use"); `skill_lattice_interface.md` §3 rule 2 (VERIFIED_STRUCTURE ≠ PERMITTED_APPLICATION); `insight_trigger_policy.md` T5 gate ("explicit user confirmation MANDATORY"); MLG `flag_identity_level_claim` op; `insight_node_schema.json` `identity_level_flag` (default quarantine) | Identity flag exists on every insight node; Q2 tier defined; T5 requires confirmation before ANY use; HEB consumes identity claims only post-confirmation | Q2 tier present verified by verify_phase4.py check 10 ("Q2_IDENTITY_LEVEL" in quarantine_tiers). Confirmation-before-use semantics **GAP** (policy-level, runtime-enforced by design) |
| Q3: user rejection is absolute | `lattice_index.json` quarantine_tiers `Q3_REJECTED` ("removed from active use regardless of evidence strength"); `skill_lattice_interface.md` §3 rule 3; `insight_trigger_policy.md` §1.1 ("rejected blocks forever"); MLG `quarantine_insight` op + SKILL.md Empowerment Boundary ("an insight the user rejects is removed from active use regardless of evidence strength"); `User_Verdict_Override` recovery op | Rejected verdict → insight removed from active use; no trigger can re-admit it (trigger prerequisite #1: rejected blocks forever); rejection propagates through dependents | T2R registers `quarantine_insight` + `record_user_verdict` as instantiated (verified by verify_critique_revisions.py check 3 counts). The reject-blocks-forever *invariant* **GAP** — no verifier simulates a rejected verdict through the trigger prerequisites |
| Q4/Q5: manipulation- and surveillance-risk tiers | `lattice_index.json` quarantine_tiers `Q4_MANIPULATION_RISK`, `Q5_SURVEILLANCE_RISK`; `insight_trigger_policy.md` §1 prerequisite 5 (both risks must be `low`, else quarantined for governance review) | Tiers defined; trigger prerequisites gate on manipulation_risk/surveillance_risk; MLG state vars carry both | Tier presence verified by verify_phase4.py check 10 (Q2 checked explicitly; Q4/Q5 present in the same block — structurally verified by JSON parse). Risk-gated trigger admission **GAP** |

## L3. User verdict paths

| Check | Evidence file(s) | Pass condition | Enforcement |
|---|---|---|---|
| Verdict lifecycle is complete and first-class | `insight_node_schema.json` `user_verdict` enum `pending|confirmed|rejected|revised`; `lattice_index.json` state_variables (`user_verdict`, `user_verdict_record`); MLG ops `present_lattice_for_review` + `record_user_verdict`; `insight_validation_protocol.md` Gate B #1 (confirmed/revised required for action influence) | Every insight carries a verdict; lattice renders for review before verdicts are recorded; no insight influences action while `pending`; verdict history retained | Ops registered in T2R (instantiated); enum coherence between schema and index is structurally verified (both JSON-parse clean, verify_phase4.py check 2). The pending-blocks-action rule **GAP** (trigger prerequisite #1 is declared in insight_trigger_policy.md, not executed by any verifier) |
| Verdicts drive calibration | `insight_validation_protocol.md` Calibration Rules (rejection rate > 30% over rolling 8 insights ⇒ halt lattice generation; user-confirmed insight replaces conflicting agent hypothesis; user wording becomes primary citation) | Calibration triggers defined and bound to verdict records | **GAP** — calibration thresholds are documented policy only; no verifier or runtime check enforces the 30% halt or the replacement rule |

## L4. Evidence-edge provenance

| Check | Evidence file(s) | Pass condition | Enforcement |
|---|---|---|---|
| Every insight cites observations via typed edges | `evidence_edge_schema.json` required `[id, from_insight, to_observation, strength, provenance, direction]`; `insight_node_schema.json` `evidence_edges` (array of observation IDs); `lattice_index.json` evidence_edge layer; MLG op `link_evidence_edges`; MLG guardrail "uncited insights are discarded" | Evidence edge schema requires provenance + direction; insight nodes require non-empty evidence_edges for admission (Gate A #2) | Schema existence + layer refs verified by verify_phase4.py check 10; JSON validity by check 2. Non-empty-citations admission rule **GAP** — no verifier checks that a produced insight has ≥1 edge |
| Observation consent is bounded | `observation_schema.json` `consent_scope` enum `explicit|implied_for_task|opt_in_pending` (opt_in_pending cannot be latticed); `insight_validation_protocol.md` Calibration rule ("validation never expands the consent scope"); MLG guardrail (observation is opt-in and bounded to agreed material) | Consent scope required on every observation; non-consented material cannot enter the lattice | Schema field presence structurally verified (JSON parse). Consent-scope enforcement **GAP** |
| Provenance carries evidence anchors | `insight_node_schema.json` `source_influences` ("Lim 2025 transition dynamics", "AnnoMI pattern"); evidence edge `provenance` (source path/bundle for auditability); `lattice_index.json` trigger flags (RECONSTRUCTED / VERIFIED per trigger) | Insight nodes can name their evidence anchors; edges carry auditable provenance paths; trigger entries flag their grounding | Flag discipline present in lattice_index insight_triggers (all 6 carry `flag`); provenance field presence structural only — **GAP** for runtime auditability check |

## L5. Trigger-policy T1–T6 compliance

| Check | Evidence file(s) | Pass condition | Enforcement |
|---|---|---|---|
| T1 (TDF/COMB): gap signal, confidence ≥ medium, verdict confirmed | `insight_trigger_policy.md` §2 T1; `lattice_index.json` insight_triggers[0] | Thresholds + gate match between policy and index (both: C-Ps gap across ≥2 observations; confidence ≥ medium; user_verdict = confirmed) | Index trigger count ≥5 verified by verify_phase4.py check 10. Per-trigger policy↔index *content parity* (T1..T6 names, confidence gates, confirmation requirements) is **GAP** — no verifier compares the two files' trigger entries |
| T2 (SDT): regulatory style, labeled inference | `insight_trigger_policy.md` §2 T2; index triggers[1] (Li 2025 / Lim 2025 VERIFIED flags) | Classification labeled and user-correctable; no guilt/shame levers; confidence ≥ low | Same as above — parity **GAP** |
| T3 (MI): change-talk via evocation only | `insight_trigger_policy.md` §2 T3; index triggers[2] (Lim 2025 HMM dynamics) | "Never as accusation" evocation gate declared; confidence ≥ low engagement / ≥ medium targeting | Same — parity **GAP** |
| T4 (PPS): atrophy risk, objective telemetry | `insight_trigger_policy.md` §2 T4; index triggers[3] (Bastani 2025, Budzyń 2025 VERIFIED) | Telemetry-over-self-report rule; falling skill_load_trend triggers, single-point dips do not; **depends on the pending skill_load_score→trend conversion (Skill_Package_QA_Checklist R2)** | Parity **GAP**; trend dependency tracked in T2R canonical_state_variables + HEB state_schema (see R2) |
| T5 (HEB): identity-level, structural trigger | `insight_trigger_policy.md` §2 T5; index triggers[4] (Valens quarantine law transposed) | Trigger is structural not confidence-based; Q2 + explicit confirmation MANDATORY | Parity **GAP**; Q2 tier existence verified by verify_phase4.py check 10 |
| T6 (deferred): practice-theory skills | `insight_trigger_policy.md` §2 T6; index triggers[5] (Material_Arrangement_Scan, Feedback_Ecology_Map deferred) | Edges quarantined in skill_graph_index until packages built; no active trigger | Deferred nodes + quarantined edges verified by verify_phase4.py check 8 |
| Universal prerequisites hold | `insight_trigger_policy.md` §1 (verdict ≠ pending, quarantine active/cleared, sufficiency ≥ partial, HEB gate passed, risks low) | Five prerequisites declared before any trigger; Q2 needs `cleared` | Declared in policy; execution **GAP** — no verifier or runtime gate evaluates the conjunction (see L2/L3) |
| Prediction ceilings constrain autonomy | `insight_trigger_policy.md` §3 (Shaikh 2026 17.1–26% timing-only; Han 2026 52.6% human review; ProEvent/PROBE 26–40% restraint; Lim 2025 0.80 session-level) | Ceilings wired into policy; low-confidence signals inform timing only, never license action | **GAP** — documented in policy only; no check that a trigger consumes a ceiling |

## L6. No-manipulation / no-surveillance

| Check | Evidence file(s) | Pass condition | Enforcement |
|---|---|---|---|
| No manipulation | `insight_trigger_policy.md` §4 prohibitions 1, 4, 5, 6, 7 (no agent-chosen-outcome steering, no reward mechanics on intrinsic motivation, no MI without spirit, no arguing resistance, no autonomous action on low confidence); `skill_lattice_interface.md` §3 rule 4; MLG guardrails (no covert steering, no self-fulfilling lattice); HEB ops `assess_motivational_inference_risk` + `assess_empowerment_theater_risk`; ConvoDojo anti-sycophancy invariant (`check_psychological_safety` reads `sycophancy_risk`) | Prohibitions enumerated; manipulation_risk state gates trigger admission (§1 #5); HEB assesses inference risk before lattice-steered action; sparring persona never scripted from lattice insights (Ma 2025) | Op existence verified (verify_packages.py check 5; verify_critique_revisions.py check 1 for check_psychological_safety). Prohibition *enforcement* **GAP** — policy-level, runtime-enforced by design |
| No surveillance | `insight_trigger_policy.md` §4 prohibition 2; `skill_lattice_interface.md` §3 rule 5; MLG guardrail (practice/chat logs never become surveillance); PPS guardrail (no surveillance-style tracking; unassisted track is a calibration instrument, not a verdict); ConvoDojo guardrail (practice data stays in practice); `observation_schema.json` consent_scope; `lattice_index.json` `Q5_SURVEILLANCE_RISK` | Observations bounded to consent scope; surveillance_risk = high blocks the observation; practice logs never feed user modeling without consent | Tier + consent field presence structurally verified. Risk-state enforcement **GAP** |
| Anti-sycophancy in sparring mode (Phase-8 #9) | ConvoDojo `SKILL.md` Guardrails + `check_psychological_safety` op + `sycophancy_risk`/`psychological_safety` state; MI guardrail (no fabricated/sycophantic change talk); MLG guardrail (persona stays persona) | Sycophancy is an orchestration invariant, not a style choice; safety op reads sycophancy_risk; rubric = lens, never verdict (Han 2026 ~52.6% auto-coding) | verified by verify_critique_revisions.py check 1 (op present + reads sycophancy_risk); invariant behavior **GAP** |
| Evidence/interpretation/implication/selection separation (Phase-8 #10) | MLG ops `separate_inference_layers` + `derive_implication_and_action`; `insight_node_schema.json` (`layer` const; `implication`/`proposed_action` as derived, non-binding fields); COMB `Separate_Evidence_Interpretation_Implication` recovery op; TDF `separate_evidence_interpretation` op | Four layers separated in schema and ops; implication/action marked hypothesis-derived and non-binding | Schema const layer verified structurally; separation invariant **GAP** |

## L7. Lattice–skill interface consistency

| Check | Evidence file(s) | Pass condition | Enforcement |
|---|---|---|---|
| Lattice_skill_edges match MLG edge_map | `lattice_index.json` lattice_skill_edges (MLG_supports [SDT, HEB]; MLG_can_follow_into [ConvoDojo, MI, COMB, TDF]; MLG_compatible_with [HEB, SDT, PPS, COMB]) vs `Motivational_Lattice_Generator/edge_map.json` (supports SDT/HEB; can_follow ConvoDojo/MI/COMB/TDF; compatible_with HEB/SDT/PPS/COMB) | Sets match exactly on all three edge types | **GAP** — verify_phase4.py checks index edges for source/target keys (check 3) and recovers_with (check 4), and verify_packages.py check 7 checks *skill_graph_index* agreement, but nothing compares `lattice_index.lattice_skill_edges` against the MLG package edge_map directly. Manual comparison 2026-08-06: consistent (all three sets agree) |
| Node-kind and edge-type domains agree | `skill_lattice_interface.md` §1.2 node kinds (skill/atomic_op/recovery_op/observation/insight/evidence_edge) + §1.3 edge domains (decomposes_to, can_follow, compatible_with, supports, recovers_with) vs skill_graph_index edges + package edge_maps | Every node kind and edge type used in the graph is declared in the interface; direction semantics documented | Direction-sensitive edge semantics verified for recovers_with by verify_phase4.py check 4; domain declarations **GAP** (documented, not enumerated against usage) |
| Insight triggers ↔ consuming skills agree with the skill graph | `insight_trigger_policy.md` §2 T1–T6 may_trigger lists vs `skill_lattice_interface.md` §2 matrix vs `lattice_index.json` insight_triggers vs skill_graph_index nodes/edges | Same trigger→skill mapping in all three lattice files; no trigger names an unbuilt or un-declared skill (deferred ones are labeled) | Deferred triggers labeled and edges quarantined (verify_phase4.py check 8); three-way mapping parity **GAP** (manual comparison 2026-08-06: consistent) |
| Bidirectional loop is documented | `skill_lattice_interface.md` §1.1 (observations → lattice → skill graph → new observations; user verdict is the arbitration point at every pass) | Closed-loop-but-never-closed-circle flow documented; skill outputs feed back as observations | **GAP** — descriptive only; no verifier tests the feedback path (ConvoDojo practice logs, MI change_talk_log, COMB profiles → observation_schema sources) |

---

## Open Items (lattice layer)

- **Runtime lattice instances**: `lattice_index.json` open_items — lattice is SEED (schema + policy); insight nodes/evidence edges populate at runtime via MLG ops. Consequently L1–L4 checks validate schemas and policy, not live data; instance-level QA is a runtime concern (Phase 7+ harness).
- **Numeric trigger thresholds**: confidence/evidence cutoffs are declared per trigger but flagged as "calibration anchors, not study-validated norms" (insight_trigger_policy.md §6); revision requires new VERIFIED evidence.
- **skill_load_score→trend conversion** (T4 dependency): pending — see Skill_Package_QA_Checklist.md R2.
- **Verifier closure candidates**: (a) lattice_skill_edges ↔ MLG edge_map set-parity check; (b) insight_trigger_policy §2 vs lattice_index insight_triggers field-parity check (T1–T6 name, confidence gate, confirmation requirement); (c) insight node admission rule (evidence_edges non-empty, Gate A); (d) verdict lifecycle simulation (pending blocks trigger, rejected blocks forever).
