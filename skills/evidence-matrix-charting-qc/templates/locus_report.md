# LOCUS REPORT — Batch <N> (<row count> <spine name>)
Frame-keeper / QC steward · <date> · Input: `<Monstare_batch_N_charting_final.json>` (post-all-QC) vs `<Monstare_batch_N_rows.json>` (seeds) + role reports from the QC waves + prior batch memos. QC only — no re-reading of sources, no re-charting. Disciplines held: citation-is-not-evidence; Tier-P visible; S1/S7/S10 protected floors never traded; H2 as vector; phenomenology-before-calibration; burden-is-a-stake; interface integrity (source -> claim -> matrix row -> design implication -> stake consequence -> artifact/pilot rule).

## 1. claims_stronger
- (row_id, what became stronger, why) — adopted re-charts, guards absorbed into dimpl/cimpl, provenance corrections, veto-first rewrites.

## 2. claims_weaker
- (row_id, what became more conditional, why) — causal downgrades, ES demotions, cross-row-supported tags, originator-advocacy caveats, working-paper provenance.

## 3. forbidden_implications
- (row_id, implication now forbidden, which discipline forbids it) — name the forbidding discipline: Purist veto / H2-vector, Ethics S-floor (S1/S7/S10), Methodologist (citation-is-not-evidence / design-licenses scope), Phenomenologist (capture-order / non-institutionalizability).

## 4. stakes_moved
**none.** — or (stake, direction, evidence). Protected floors hold/strengthen only; any trade is a breach.

## 5. protected_floor_strain
- (stake, row_id, strain detail) — strain = guarded in charting, enforcement deferred to artifact level; NOT a breach. Name unnamed cross-row interfaces here (e.g., flow layer <-> MRT decision points).

## 6. apparatus_burden
One paragraph. State the ACTUAL composition (verify Pri counts yourself — the pass brief may be wrong; abstract-level count; quantitative payload), the yield (re-charts, veto rewrites, downgrades, URL catches), and the collapse recommendation per v3 §5.2.

## 7. contrary_indicators
- (row_id, disconfirming signal) — source-side limits, contested claims, transfer risks, originator-corpus reliance.

## 8. cosmotechnic_telos_check
Structural or decorative — one paragraph, three-level test: (i) criterion-derived content (Tier-P applications from stated criteria, provenance-glossed), (ii) source-grounded micro-orders (guards pinned to charted passages), (iii) artifact carriage (every dimpl terminates in a named artifact/pilot rule).

## 9. rows_to_downgrade
- (row_id, weight decision, reason) — tags appended to Discovery Notes per convention: 'LOCUS <date>: supporting weight (not load-bearing)' or 'load-bearing'. Split evidence weight from design-role load where a row's design implication is the only source of a binding rule.

## 10. re_chart_required
**None at matrix-write level.** — or row_ids. Verify adopted re-charts satisfy the flag's REQUIRED additions item-by-item. List verification debts gated to the artifact-construction pass (not the matrix write).
